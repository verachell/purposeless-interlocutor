

<%= render "nav" %>

<h1 style="text-align:center;font-size:3em">Status updates</h1>

<hr />
<turbo-frame id="turbo-updates">
<% if notice.present? %>
<p class="py-2 px-3 bg-green-50 mb-5 text-green-500 font-medium rounded-lg inline-block" id="notice">
<%= notice %>
</p>
<% end %>

<%= render "deleteall", comments: @comments %>

<%= render "form", comment: @comment %>

<div class="px-5">
<%= render @comments %>
</div>
</turbo-frame>

<hr />

<%= render "footer" %>